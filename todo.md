Okay can you do two things
1. remove the colorcoding from rows entirely
2. sort the columns by "In-Game Size". This should update when we recalculate the "Total In game size"



CRASH: Open virtua cop 2.zip,  click "start button 2" and set 1024x1024 to 8x16. it freezes when we set the "16."

If a texture doesn't need to be resized can you just make the preview texture window (NOT the 3D window) be greyed out? however, if it DOES get changed (like it was a 1024 and then the user made it a 128) it should appear in its new form.


We have a way to detect single color textures but do we have a way to detect stuff htat's really stupid like 4 colors in a texture that are just blocks or something? Algorithmically how do we discover dumb stuff like this?


This is a weird request, but people are shipping Custom Screen Types that are not 4:3 aspect ratio, they are fucking it up and doing like 1:1 or even worse 16:9 when most arcade games are 4:3. is there a way to do some sort of quick check on a screentype:custom to catch meshes that are not 4:3?

check screen is 4:3

ures with a NICE resize algorithm.

if there's no cabinet loaded it is greyed out.

p) can you make a buttton that says "Make working copy". When you click this it will

Okay, so we definitely want to let people resize from inside the tool. 

Next to (open cabinet.zip) can you make a buttton that says "Make working copy". When you click this it will 